import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Button } from "./Button";
import { ReactImageSelector } from "../components/React-Image-Selector/react-image-selector";

<Meta title="React Image Selector" component={ReactImageSelector} />

# React Image Selector

With `MDX` we can define a story for `React Image Selector` right in the middle of our
markdown documentation.

<Canvas>
  <Story name="RIS Default">
    <form>
      <Button id="Unchecked" label="Unchecked" />
      <Button id="Checked" label="Checked" checked />
      <Button appearance="secondary" id="second" label="Secondary" checked />
    </form>
  </Story>
</Canvas>
